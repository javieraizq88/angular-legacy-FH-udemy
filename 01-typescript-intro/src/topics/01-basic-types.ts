
const name : string ='javiera';

// solo acepta el string 'FULL'
let hpPoints: number | 'FULL' = 100;

const isAlive: boolean = true;

hpPoints = 'FULL';

console.log({
    name, hpPoints, isAlive
})


export {};